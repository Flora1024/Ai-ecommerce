spring:
    datasource:
        driver-class-name: com.p6spy.engine.spy.P6SpyDriver          # 数据库驱动类名
        url: jdbc:p6spy:postgresql://localhost:5432/postgres         # 数据库连接 URL
        username: postgres                                           # 数据库用户名
        password: postgres                                           # 数据库密码
        hikari:                                                      # HikariCP 连接池配置
            pool-name: AI-Robot-HikariCP                             # 自定义连接池名称
            auto-commit: true                                        # 是否自动提交事务
            connection-timeout: 30000                                # 连接超时时间（毫秒）
            idle-timeout: 600000                                     # 空闲连接存活最大时间（毫秒）
            max-lifetime: 1800000                                    # 连接最大存活时间（毫秒）
            minimum-idle: 5                                          # 最小空闲连接数
            maximum-pool-size: 20                                    # 最大连接池大小
            connection-test-query: SELECT 1                          # 连接测试查询
            validation-timeout: 5000

    # 第二个数据源 - DuckDB 内存模式
    datasource-duckdb:
        driver-class-name: org.duckdb.DuckDBDriver
        url: "jdbc:duckdb:"

    ai:
        openai:
            base-url: https://dashscope.aliyuncs.com/compatible-mode # OpenAI 服务的访问地址
            api-key: sk-800dbad219764571ab1d056c09be5fed             # 阿里云百炼 API Key
            embedding:
                options:
                    model: text-embedding-v4
                    dimensions: 1536
            chat:
                options:
                    model: qwen3.5-plus
        vectorstore:
            pgvector:
                index-type: HNSW                                     # 索引类型
                distance-type: COSINE_DISTANCE                       # 距离类型
                dimensions: 1536                                     # 嵌入维度
                max-document-batch-size: 10000                       # 每批处理的最大文档数
                table-name: t_vector_store                           # 向量表名称
    servlet:
        multipart:
            max-file-size: 50MB
            max-request-size: 100MB

okhttp:                                                              # OkHttp 客户端配置
    connect-timeout: 5000                                            # 建立连接的最大等待时间（毫秒）
    read-timeout: 30000                                              # 读取数据的最大等待时间（毫秒）
    write-timeout: 15000                                             # 写入数据的最大等待时间（毫秒）
    max-idle-connections: 200                                        # 连接池中保持的最大空闲连接数
    keep-alive-duration: 5                                           # 空闲连接在连接池中的存活时间（分钟）

searxng:                                                             # SearXNG 搜索引擎配置
    url: http://localhost:8888/search                                # SearXNG 服务的 API 端点地址
    count: 10                                                        # 每次从搜索结果中，提取的最大数量

customer-service:
    md-storage-path: D:\\Dev\\personal-projects\\xiaoha-ai-robot\\md_files           # 问答文件存储路径
    xlsx-storage-path: D:\\Dev\\personal-projects\\xiaoha-ai-robot\\xlsx_files       # 数据表文件存储路径
    model: deepseek-v3
    temperature: 0.0

